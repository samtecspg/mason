type: config
id: "1"
execution_clients: 
  - "spark"
clients:
  spark:
    configuration:
      runner:
        type: "kubernetes-operator"
        spark_version: "2.4.5"
        bad_attribute: "test"
