type: "operator"
namespace: "table"
command: "refresh"
description:  "Refresh metastore tables"
parameters:
  required:
    - "database_name"
    - "table_name"
supported_configurations:
  - scheduler: "glue"
    execution: "local"
