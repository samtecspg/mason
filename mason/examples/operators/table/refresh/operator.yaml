type: "operator"
namespace: "table"
command: "refresh"
description:  "Refresh metastore tables"
parameters:
  required:
    - table_path
supported_configurations:
  - scheduler: "glue"
    execution: "local"
