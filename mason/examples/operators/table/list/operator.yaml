type: "operator"
namespace: "table"
command: "list"
description:  "Get metastore tables"
parameters:
  required:
    - "database_name"
supported_configurations:
  - metastore: "s3"
  - metastore: "glue"
