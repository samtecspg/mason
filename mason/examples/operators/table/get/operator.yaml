type: "operator"
namespace: "table"
command: "get"
description:  "Get metastore table contents"
parameters:
  required:
    - "database_name"
    - "table_name"
  optional:
    - "read_headers"
supported_configurations:
  - metastore: "local"
    execution: "local"
  - metastore: "s3"
    execution: "local"
  - metastore: "glue"
    execution: "local"