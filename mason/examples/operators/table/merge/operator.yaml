type: "operator"
namespace: "table"
command: "merge"
description:  "Merge metastore tables"
parameters:
  required:
    - output_path
    - input_path
  optional:
    - extract_paths
    - parse_headers
supported_configurations:
  - storage: "s3"
    execution: "spark"
