type: "operator"
namespace: "table"
command: "infer"
description:  "Infers schema for metastore tables"
parameters:
  required:
    - "database_name"
    - "storage_path"
    - "schedule_name"
  optional:
    - "schedule_name"
    - "schedule_frequency"
supported_configurations:
  - metastore: "glue"
    scheduler: "glue"
    storage: "s3"
  - metastore: "athena"
    storage: "s3"
    execution: "spark"
