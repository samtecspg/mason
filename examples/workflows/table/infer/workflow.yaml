type: "workflow"
namespace: "table"
command: "infer"
description: "One step workfow for table infer operator"
parameters:
  required:
    - "database_name"
    - "storage_path"
    - "schedule_name"
    - "schedule_name"
    - "schedule"
dag:
  - type: "operator"
    namespace: "table"
    command: "infer"



#    supported_configurations:
#      - metastore: "glue"
#        scheduler: "glue"
#        storage: "s3"
#      - metastore: "athena"
#        storage: "s3"
#        execution: "spark"
